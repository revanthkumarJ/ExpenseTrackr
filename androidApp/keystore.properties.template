# Copy this file to `keystore.properties` (same folder) and fill in your values.
# keystore.properties is gitignored — never commit it or the .jks file to this public repo.
storeFile=/absolute/path/to/expensetrackr-release.jks
storePassword=YOUR_KEYSTORE_PASSWORD
keyAlias=expensetrackr
keyPassword=YOUR_KEY_PASSWORD
